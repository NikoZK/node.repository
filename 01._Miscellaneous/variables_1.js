//Node REPL

//type coercion
//strict equality

//Rule 1: use const per default unless it needs to change, then use let

const luckyNumber = 10;

//const is constant in the assignment 

console.log(luckyNumber)

//JS Data types
//String, Number, BigInt, Boolean, Object, Symbol, null, undefined

const person = {
    name: "Niko" //key-value pair
};

person.name = "NikoZK"
person.age = 24

console.log(person)

const people = []
people.push("Victor") //Add to list
console.log(people)